# Beibei Home Textile Miniprogram Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the first native WeChat Mini Program version for 贝贝家纺: product display, category browsing, product details, store information, and contact actions.

**Architecture:** Use a native WeChat Mini Program under `miniprogram/`. Keep product, category, and store data in local JavaScript modules. Share contact behavior through a small utility and reuse product/contact UI through focused components.

**Tech Stack:** WeChat Mini Program native `WXML`, `WXSS`, `JavaScript`, local CommonJS modules, WeChat APIs `wx.navigateTo`, `wx.switchTab`, `wx.makePhoneCall`, `wx.setClipboardData`, `wx.openLocation`, `wx.showToast`, and `wx.showModal`.

---

## File Structure

- Modify `project.config.json`: WeChat DevTools project metadata, preserving the existing AppID and adding `miniprogramRoot`.
- Create `miniprogram/app.js`: global app bootstrap.
- Create `miniprogram/app.json`: route, tabBar, window, and component registration.
- Create `miniprogram/app.wxss`: global theme, layout helpers, and typography.
- Create `miniprogram/sitemap.json`: allow indexable pages.
- Create `miniprogram/config/store.js`: store name, phone, WeChat ID, address, hours, coordinates.
- Create `miniprogram/data/categories.js`: first-version product categories.
- Create `miniprogram/data/products.js`: sample local product catalog.
- Create `miniprogram/utils/contact.js`: shared contact, phone, and navigation actions.
- Create `miniprogram/components/product-card/*`: reusable product summary card.
- Create `miniprogram/components/contact-bar/*`: reusable bottom contact action bar.
- Create `miniprogram/pages/home/*`: category-first homepage.
- Create `miniprogram/pages/products/*`: category browsing and product list.
- Create `miniprogram/pages/product-detail/*`: product detail view.
- Create `miniprogram/pages/store/*`: store information and contact view.

## Task 1: Mini Program Project Skeleton

**Files:**
- Modify: `project.config.json`
- Create: `miniprogram/app.js`
- Create: `miniprogram/app.json`
- Create: `miniprogram/app.wxss`
- Create: `miniprogram/sitemap.json`

- [ ] **Step 1: Update WeChat project config**

Update `project.config.json`. Preserve the current `appid` value already generated by WeChat DevTools, and make sure the file contains this structure:

```json
{
  "description": "贝贝家纺微信小程序",
  "packOptions": {
    "ignore": []
  },
  "setting": {
    "urlCheck": false,
    "es6": true,
    "enhance": true,
    "postcss": true,
    "minified": true
  },
  "compileType": "miniprogram",
  "libVersion": "3.16.0",
  "appid": "wx4d5280f1c259e088",
  "projectname": "bbjf",
  "miniprogramRoot": "miniprogram/",
  "condition": {}
}
```

- [ ] **Step 2: Create app bootstrap**

Add `miniprogram/app.js`:

```js
App({
  globalData: {
    storeName: "贝贝家纺"
  }
});
```

- [ ] **Step 3: Create app route config**

Add `miniprogram/app.json`:

```json
{
  "pages": [
    "pages/home/home",
    "pages/products/products",
    "pages/product-detail/product-detail",
    "pages/store/store"
  ],
  "window": {
    "navigationBarTitleText": "贝贝家纺",
    "navigationBarBackgroundColor": "#9b3430",
    "navigationBarTextStyle": "white",
    "backgroundColor": "#f7f4ef"
  },
  "tabBar": {
    "color": "#7a7167",
    "selectedColor": "#9b3430",
    "backgroundColor": "#ffffff",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/home/home",
        "text": "首页"
      },
      {
        "pagePath": "pages/products/products",
        "text": "产品"
      },
      {
        "pagePath": "pages/store/store",
        "text": "门店"
      }
    ]
  },
  "usingComponents": {}
}
```

- [ ] **Step 4: Create global styles**

Add `miniprogram/app.wxss`:

```css
page {
  background: #f7f4ef;
  color: #252525;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

.page {
  min-height: 100vh;
  padding: 24rpx;
  box-sizing: border-box;
}

.section {
  margin-top: 24rpx;
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.section-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #2f2b27;
}

.section-link {
  font-size: 24rpx;
  color: #9b3430;
}

.card {
  background: #ffffff;
  border-radius: 12rpx;
  box-shadow: 0 10rpx 28rpx rgba(58, 45, 35, 0.08);
}

.muted {
  color: #7a7167;
}

.safe-bottom {
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
}
```

- [ ] **Step 5: Create sitemap config**

Add `miniprogram/sitemap.json`:

```json
{
  "rules": [
    {
      "action": "allow",
      "page": "*"
    }
  ]
}
```

- [ ] **Step 6: Verify JSON files parse**

Run:

```powershell
Get-Content project.config.json | ConvertFrom-Json | Out-Null
Get-Content miniprogram/app.json | ConvertFrom-Json | Out-Null
Get-Content miniprogram/sitemap.json | ConvertFrom-Json | Out-Null
```

Expected: exit code `0`, no parse errors.

- [ ] **Step 7: Commit skeleton**

Run:

```powershell
git add project.config.json miniprogram/app.js miniprogram/app.json miniprogram/app.wxss miniprogram/sitemap.json
git commit -m "chore: 初始化微信小程序项目骨架"
```

## Task 2: Local Data and Contact Utilities

**Files:**
- Create: `miniprogram/config/store.js`
- Create: `miniprogram/data/categories.js`
- Create: `miniprogram/data/products.js`
- Create: `miniprogram/utils/contact.js`

- [ ] **Step 1: Add store config**

Add `miniprogram/config/store.js`:

```js
const store = {
  name: "贝贝家纺",
  slogan: "实体门店 · 成品床品 · 到店可看实物",
  phone: "",
  wechatId: "",
  address: "请补充门店完整地址",
  businessHours: "请补充营业时间",
  latitude: null,
  longitude: null,
  shareTitle: "贝贝家纺成品床品，到店可看实物"
};

module.exports = store;
```

- [ ] **Step 2: Add categories**

Add `miniprogram/data/categories.js`:

```js
const categories = [
  { id: "sets", name: "床品四件套", sort: 1, description: "日常家用，多花色可选" },
  { id: "quilts", name: "被芯", sort: 2, description: "四季被、冬被、夏凉被" },
  { id: "pillows", name: "枕芯", sort: 3, description: "舒适支撑，软硬可选" },
  { id: "blankets", name: "毯子", sort: 4, description: "毛毯、盖毯、午睡毯" },
  { id: "mats", name: "凉席", sort: 5, description: "夏季清凉，到店挑选" },
  { id: "wedding", name: "婚庆套件", sort: 6, description: "婚庆床品，红色喜庆款" },
  { id: "specials", name: "特价专区", sort: 7, description: "近期优惠，数量有限" }
];

module.exports = categories;
```

- [ ] **Step 3: Add product catalog**

Add `miniprogram/data/products.js`:

```js
const products = [
  {
    id: "cotton-set-001",
    categoryId: "sets",
    name: "纯棉四件套",
    tags: ["热卖", "纯棉"],
    priceType: "range",
    priceText: "¥199 起",
    imageTone: "warm",
    highlights: ["亲肤透气", "多花色可选", "到店可看实物"],
    specs: ["适用床型：1.5m / 1.8m", "材质：纯棉", "颜色：多款可选"],
    description: "适合日常家用的成品床品，具体花色和库存以到店为准。",
    isFeatured: true,
    isSpecial: false,
    isNew: false
  },
  {
    id: "winter-quilt-001",
    categoryId: "quilts",
    name: "保暖冬被芯",
    tags: ["热卖", "保暖"],
    priceType: "consult",
    priceText: "到店咨询价",
    imageTone: "deep",
    highlights: ["蓬松保暖", "多尺寸可选", "适合秋冬"],
    specs: ["尺寸：多规格", "厚度：冬被", "库存：以门店为准"],
    description: "秋冬保暖被芯，适合到店根据床型和厚度需求挑选。",
    isFeatured: true,
    isSpecial: false,
    isNew: false
  },
  {
    id: "pillow-001",
    categoryId: "pillows",
    name: "舒睡枕芯",
    tags: ["舒适", "成品"],
    priceType: "consult",
    priceText: "微信咨询价",
    imageTone: "soft",
    highlights: ["软硬适中", "日常家用", "可搭配床品"],
    specs: ["规格：单只装", "高度：多款可选", "材质：以实物为准"],
    description: "日常睡眠枕芯，可到店试摸手感后选择。",
    isFeatured: false,
    isSpecial: false,
    isNew: true
  },
  {
    id: "blanket-001",
    categoryId: "blankets",
    name: "亲肤毛毯",
    tags: ["现货", "亲肤"],
    priceType: "range",
    priceText: "¥89 起",
    imageTone: "rose",
    highlights: ["触感柔软", "居家午睡", "多颜色可选"],
    specs: ["尺寸：多规格", "用途：盖毯 / 午睡毯", "颜色：到店选择"],
    description: "适合沙发、卧室、午睡等场景，门店可看实物颜色。",
    isFeatured: false,
    isSpecial: false,
    isNew: true
  },
  {
    id: "summer-mat-001",
    categoryId: "mats",
    name: "夏季凉席",
    tags: ["夏季", "清凉"],
    priceType: "consult",
    priceText: "到店咨询价",
    imageTone: "green",
    highlights: ["夏季清凉", "多尺寸", "适合到店挑选"],
    specs: ["适用床型：1.5m / 1.8m", "款式：多款可选", "库存：以门店为准"],
    description: "夏季家用凉席，建议到店根据床型尺寸选择。",
    isFeatured: false,
    isSpecial: false,
    isNew: false
  },
  {
    id: "wedding-set-001",
    categoryId: "wedding",
    name: "婚庆四件套",
    tags: ["婚庆", "喜庆"],
    priceType: "consult",
    priceText: "到店咨询价",
    imageTone: "red",
    highlights: ["喜庆配色", "成品套件", "适合新婚布置"],
    specs: ["风格：婚庆", "床型：多规格", "花色：以门店为准"],
    description: "婚庆成品床品，适合到店看花色和手感。",
    isFeatured: true,
    isSpecial: false,
    isNew: false
  },
  {
    id: "special-set-001",
    categoryId: "specials",
    name: "特价床品套件",
    tags: ["特价", "限量"],
    priceType: "special",
    priceText: "特价 ¥159 起",
    imageTone: "gold",
    highlights: ["近期优惠", "数量有限", "到店看实物"],
    specs: ["品类：床品套件", "价格：以门店活动为准", "数量：售完为止"],
    description: "门店近期特价款，具体花色、尺寸和数量以门店为准。",
    isFeatured: true,
    isSpecial: true,
    isNew: false
  }
];

module.exports = products;
```

- [ ] **Step 4: Add contact utility**

Add `miniprogram/utils/contact.js`:

```js
const store = require("../config/store");

function copyWechat() {
  if (!store.wechatId) {
    wx.showModal({
      title: "微信咨询",
      content: "微信号还未配置。正式发布前请补充门店微信号或客服入口。",
      showCancel: false
    });
    return;
  }

  wx.setClipboardData({
    data: store.wechatId,
    success() {
      wx.showToast({ title: "微信号已复制", icon: "success" });
    }
  });
}

function callStore() {
  if (!store.phone) {
    wx.showModal({
      title: "电话联系",
      content: "联系电话还未配置。正式发布前请补充门店电话。",
      showCancel: false
    });
    return;
  }

  wx.makePhoneCall({ phoneNumber: store.phone });
}

function openStoreLocation() {
  if (typeof store.latitude !== "number" || typeof store.longitude !== "number") {
    wx.showModal({
      title: "导航到店",
      content: "门店经纬度还未配置。正式发布前请补充准确位置。",
      showCancel: false
    });
    return;
  }

  wx.openLocation({
    latitude: store.latitude,
    longitude: store.longitude,
    name: store.name,
    address: store.address,
    scale: 18
  });
}

module.exports = {
  copyWechat,
  callStore,
  openStoreLocation
};
```

- [ ] **Step 5: Verify modules load in Node syntax**

Run:

```powershell
node -e "const categories=require('./miniprogram/data/categories'); const products=require('./miniprogram/data/products'); const store=require('./miniprogram/config/store'); if(!categories.length||!products.length||store.name!=='贝贝家纺') process.exit(1); console.log(categories.length, products.length, store.name)"
```

Expected: exit code `0`, output includes `7 7 贝贝家纺`.

- [ ] **Step 6: Commit data and utilities**

Run:

```powershell
git add miniprogram/config/store.js miniprogram/data/categories.js miniprogram/data/products.js miniprogram/utils/contact.js
git commit -m "feat: 添加门店配置和商品数据"
```

## Task 3: Shared Components

**Files:**
- Create: `miniprogram/components/product-card/product-card.js`
- Create: `miniprogram/components/product-card/product-card.json`
- Create: `miniprogram/components/product-card/product-card.wxml`
- Create: `miniprogram/components/product-card/product-card.wxss`
- Create: `miniprogram/components/contact-bar/contact-bar.js`
- Create: `miniprogram/components/contact-bar/contact-bar.json`
- Create: `miniprogram/components/contact-bar/contact-bar.wxml`
- Create: `miniprogram/components/contact-bar/contact-bar.wxss`

- [ ] **Step 1: Add product card component JS**

Add `miniprogram/components/product-card/product-card.js`:

```js
Component({
  properties: {
    product: {
      type: Object,
      value: {}
    }
  },
  methods: {
    handleTap() {
      const id = this.data.product && this.data.product.id;
      if (!id) return;

      wx.navigateTo({
        url: `/pages/product-detail/product-detail?id=${id}`
      });
    }
  }
});
```

- [ ] **Step 2: Add product card component config**

Add `miniprogram/components/product-card/product-card.json`:

```json
{
  "component": true
}
```

- [ ] **Step 3: Add product card markup**

Add `miniprogram/components/product-card/product-card.wxml`:

```xml
<view class="product-card card" bindtap="handleTap">
  <view class="product-cover tone-{{product.imageTone || 'warm'}}">
    <text wx:if="{{product.tags && product.tags.length}}" class="tag">{{product.tags[0]}}</text>
  </view>
  <view class="product-info">
    <view class="product-name">{{product.name}}</view>
    <view class="product-highlights">{{product.highlights[0]}}</view>
    <view class="product-price {{product.priceType === 'special' ? 'special' : ''}}">{{product.priceText}}</view>
  </view>
</view>
```

- [ ] **Step 4: Add product card styles**

Add `miniprogram/components/product-card/product-card.wxss`:

```css
.product-card {
  overflow: hidden;
}

.product-cover {
  position: relative;
  height: 180rpx;
}

.tone-warm { background: linear-gradient(135deg, #ead7c5, #b85648); }
.tone-deep { background: linear-gradient(135deg, #d6c4a8, #6f3f2c); }
.tone-soft { background: linear-gradient(135deg, #f2dedc, #c87982); }
.tone-rose { background: linear-gradient(135deg, #f1c7c0, #a94c5a); }
.tone-green { background: linear-gradient(135deg, #d8e5cf, #5e8f74); }
.tone-red { background: linear-gradient(135deg, #e3a09a, #9b3430); }
.tone-gold { background: linear-gradient(135deg, #edd59d, #c8872d); }

.tag {
  position: absolute;
  top: 12rpx;
  left: 12rpx;
  padding: 4rpx 12rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.88);
  color: #9b3430;
  font-size: 22rpx;
  font-weight: 700;
}

.product-info {
  padding: 16rpx;
}

.product-name {
  color: #2f2b27;
  font-size: 28rpx;
  font-weight: 700;
}

.product-highlights {
  margin-top: 8rpx;
  color: #7a7167;
  font-size: 22rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.product-price {
  margin-top: 12rpx;
  color: #9b3430;
  font-size: 26rpx;
  font-weight: 700;
}

.product-price.special {
  color: #c46a20;
}
```

- [ ] **Step 5: Add contact bar component**

Add `miniprogram/components/contact-bar/contact-bar.js`:

```js
const contact = require("../../utils/contact");

Component({
  methods: {
    handleWechat() {
      contact.copyWechat();
    },
    handlePhone() {
      contact.callStore();
    },
    handleLocation() {
      contact.openStoreLocation();
    }
  }
});
```

- [ ] **Step 6: Add contact bar component config**

Add `miniprogram/components/contact-bar/contact-bar.json`:

```json
{
  "component": true
}
```

- [ ] **Step 7: Add contact bar markup**

Add `miniprogram/components/contact-bar/contact-bar.wxml`:

```xml
<view class="contact-bar">
  <button class="contact-button primary" bindtap="handleWechat">微信咨询</button>
  <button class="contact-button" bindtap="handlePhone">电话联系</button>
  <button class="contact-button" bindtap="handleLocation">导航到店</button>
</view>
```

- [ ] **Step 8: Add contact bar styles**

Add `miniprogram/components/contact-bar/contact-bar.wxss`:

```css
.contact-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 20;
  display: grid;
  grid-template-columns: 1.2fr 1fr 1fr;
  gap: 12rpx;
  padding: 16rpx 20rpx calc(16rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.96);
  box-shadow: 0 -8rpx 24rpx rgba(58, 45, 35, 0.08);
}

.contact-button {
  height: 76rpx;
  line-height: 76rpx;
  padding: 0;
  border-radius: 10rpx;
  background: #f0ebe3;
  color: #2f2b27;
  font-size: 26rpx;
  font-weight: 700;
}

.contact-button::after {
  border: 0;
}

.contact-button.primary {
  background: #9b3430;
  color: #ffffff;
}
```

- [ ] **Step 9: Commit shared components**

Run:

```powershell
git add miniprogram/components
git commit -m "feat: 添加商品卡片和联系栏组件"
```

## Task 4: Home Page

**Files:**
- Create: `miniprogram/pages/home/home.js`
- Create: `miniprogram/pages/home/home.json`
- Create: `miniprogram/pages/home/home.wxml`
- Create: `miniprogram/pages/home/home.wxss`

- [ ] **Step 1: Add home page JS**

Add `miniprogram/pages/home/home.js`:

```js
const store = require("../../config/store");
const categories = require("../../data/categories");
const products = require("../../data/products");
const contact = require("../../utils/contact");

Page({
  data: {
    store,
    categories: [],
    featuredProducts: [],
    specialProducts: []
  },
  onLoad() {
    this.setData({
      categories: categories.slice().sort((a, b) => a.sort - b.sort),
      featuredProducts: products.filter((item) => item.isFeatured).slice(0, 4),
      specialProducts: products.filter((item) => item.isSpecial).slice(0, 2)
    });
  },
  onShareAppMessage() {
    return {
      title: store.shareTitle,
      path: "/pages/home/home"
    };
  },
  handleWechat() {
    contact.copyWechat();
  },
  handlePhone() {
    contact.callStore();
  },
  handleLocation() {
    contact.openStoreLocation();
  },
  openCategory(event) {
    const id = event.currentTarget.dataset.id;
    wx.switchTab({
      url: "/pages/products/products",
      success() {
        wx.setStorageSync("selectedCategoryId", id);
      }
    });
  },
  openProducts() {
    wx.switchTab({ url: "/pages/products/products" });
  }
});
```

- [ ] **Step 2: Add home page config**

Add `miniprogram/pages/home/home.json`:

```json
{
  "navigationBarTitleText": "贝贝家纺",
  "usingComponents": {
    "product-card": "/components/product-card/product-card"
  }
}
```

- [ ] **Step 3: Add home page markup**

Add `miniprogram/pages/home/home.wxml`:

```xml
<view class="page home-page">
  <view class="hero">
    <view class="hero-copy">
      <view class="store-name">{{store.name}}</view>
      <view class="store-slogan">{{store.slogan}}</view>
    </view>
    <view class="hero-note">花色、尺寸、库存以门店实物为准</view>
  </view>

  <view class="quick-actions">
    <button bindtap="handleWechat">微信咨询</button>
    <button bindtap="handlePhone">电话联系</button>
    <button bindtap="handleLocation">导航到店</button>
  </view>

  <view class="section">
    <view class="section-header">
      <view class="section-title">产品分类</view>
      <view class="section-link" bindtap="openProducts">全部产品</view>
    </view>
    <view class="category-grid">
      <view
        wx:for="{{categories}}"
        wx:key="id"
        class="category-card card"
        data-id="{{item.id}}"
        bindtap="openCategory"
      >
        <view class="category-name">{{item.name}}</view>
        <view class="category-desc">{{item.description}}</view>
      </view>
    </view>
  </view>

  <view class="section" wx:if="{{specialProducts.length}}">
    <view class="section-header">
      <view class="section-title">特价专区</view>
      <view class="section-link" bindtap="openProducts">查看更多</view>
    </view>
    <view class="product-grid">
      <product-card wx:for="{{specialProducts}}" wx:key="id" product="{{item}}" />
    </view>
  </view>

  <view class="section">
    <view class="section-header">
      <view class="section-title">热卖推荐</view>
      <view class="section-link" bindtap="openProducts">更多</view>
    </view>
    <view class="product-grid">
      <product-card wx:for="{{featuredProducts}}" wx:key="id" product="{{item}}" />
    </view>
  </view>
</view>
```

- [ ] **Step 4: Add home page styles**

Add `miniprogram/pages/home/home.wxss`:

```css
.home-page {
  padding-bottom: 48rpx;
}

.hero {
  min-height: 260rpx;
  padding: 34rpx;
  border-radius: 16rpx;
  background: linear-gradient(135deg, #9b3430, #d58a48);
  color: #ffffff;
}

.store-name {
  font-size: 48rpx;
  font-weight: 800;
}

.store-slogan {
  margin-top: 12rpx;
  font-size: 26rpx;
  opacity: 0.94;
}

.hero-note {
  margin-top: 42rpx;
  font-size: 24rpx;
  opacity: 0.9;
}

.quick-actions {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx;
  margin-top: 20rpx;
}

.quick-actions button {
  height: 82rpx;
  line-height: 82rpx;
  padding: 0;
  border-radius: 12rpx;
  background: #ffffff;
  color: #9b3430;
  font-size: 26rpx;
  font-weight: 700;
}

.quick-actions button::after {
  border: 0;
}

.category-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16rpx;
}

.category-card {
  padding: 22rpx;
}

.category-name {
  color: #2f2b27;
  font-size: 30rpx;
  font-weight: 700;
}

.category-desc {
  margin-top: 10rpx;
  color: #7a7167;
  font-size: 23rpx;
  line-height: 1.45;
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16rpx;
}
```

- [ ] **Step 5: Verify home page files exist**

Run:

```powershell
Test-Path miniprogram/pages/home/home.js
Test-Path miniprogram/pages/home/home.wxml
Test-Path miniprogram/pages/home/home.wxss
```

Expected: three `True` lines.

- [ ] **Step 6: Commit home page**

Run:

```powershell
git add miniprogram/pages/home
git commit -m "feat: 添加品类优先首页"
```

## Task 5: Products Page

**Files:**
- Create: `miniprogram/pages/products/products.js`
- Create: `miniprogram/pages/products/products.json`
- Create: `miniprogram/pages/products/products.wxml`
- Create: `miniprogram/pages/products/products.wxss`

- [ ] **Step 1: Add products page JS**

Add `miniprogram/pages/products/products.js`:

```js
const categories = require("../../data/categories");
const products = require("../../data/products");

function getProductsByCategory(categoryId) {
  return products.filter((item) => item.categoryId === categoryId);
}

Page({
  data: {
    categories: [],
    selectedCategoryId: "",
    visibleProducts: []
  },
  onLoad() {
    const sortedCategories = categories.slice().sort((a, b) => a.sort - b.sort);
    const selectedCategoryId = sortedCategories[0] ? sortedCategories[0].id : "";
    this.setData({
      categories: sortedCategories,
      selectedCategoryId,
      visibleProducts: getProductsByCategory(selectedCategoryId)
    });
  },
  onShow() {
    const storedCategoryId = wx.getStorageSync("selectedCategoryId");
    if (!storedCategoryId) return;

    wx.removeStorageSync("selectedCategoryId");
    this.selectCategoryById(storedCategoryId);
  },
  selectCategory(event) {
    this.selectCategoryById(event.currentTarget.dataset.id);
  },
  selectCategoryById(categoryId) {
    const exists = categories.some((item) => item.id === categoryId);
    if (!exists) return;

    this.setData({
      selectedCategoryId: categoryId,
      visibleProducts: getProductsByCategory(categoryId)
    });
  }
});
```

- [ ] **Step 2: Add products page config**

Add `miniprogram/pages/products/products.json`:

```json
{
  "navigationBarTitleText": "产品展示",
  "usingComponents": {
    "product-card": "/components/product-card/product-card"
  }
}
```

- [ ] **Step 3: Add products page markup**

Add `miniprogram/pages/products/products.wxml`:

```xml
<view class="page products-page">
  <scroll-view class="category-tabs" scroll-x enable-flex>
    <view
      wx:for="{{categories}}"
      wx:key="id"
      class="category-tab {{selectedCategoryId === item.id ? 'active' : ''}}"
      data-id="{{item.id}}"
      bindtap="selectCategory"
    >
      {{item.name}}
    </view>
  </scroll-view>

  <view wx:if="{{visibleProducts.length}}" class="product-grid">
    <product-card wx:for="{{visibleProducts}}" wx:key="id" product="{{item}}" />
  </view>

  <view wx:else class="empty card">
    <view class="empty-title">商品正在整理中</view>
    <view class="empty-desc">该分类商品正在整理中，可先微信咨询或到店看看。</view>
  </view>
</view>
```

- [ ] **Step 4: Add products page styles**

Add `miniprogram/pages/products/products.wxss`:

```css
.products-page {
  padding-bottom: 48rpx;
}

.category-tabs {
  white-space: nowrap;
  margin: -4rpx -24rpx 24rpx;
  padding: 4rpx 24rpx 12rpx;
  box-sizing: border-box;
}

.category-tab {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 68rpx;
  margin-right: 12rpx;
  padding: 0 24rpx;
  border-radius: 999rpx;
  background: #ffffff;
  color: #5f574f;
  font-size: 26rpx;
  font-weight: 700;
}

.category-tab.active {
  background: #9b3430;
  color: #ffffff;
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16rpx;
}

.empty {
  padding: 42rpx 28rpx;
  text-align: center;
}

.empty-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #2f2b27;
}

.empty-desc {
  margin-top: 12rpx;
  color: #7a7167;
  font-size: 24rpx;
  line-height: 1.6;
}
```

- [ ] **Step 5: Commit products page**

Run:

```powershell
git add miniprogram/pages/products
git commit -m "feat: 添加产品分类浏览页"
```

## Task 6: Product Detail Page

**Files:**
- Create: `miniprogram/pages/product-detail/product-detail.js`
- Create: `miniprogram/pages/product-detail/product-detail.json`
- Create: `miniprogram/pages/product-detail/product-detail.wxml`
- Create: `miniprogram/pages/product-detail/product-detail.wxss`

- [ ] **Step 1: Add product detail JS**

Add `miniprogram/pages/product-detail/product-detail.js`:

```js
const store = require("../../config/store");
const products = require("../../data/products");

Page({
  data: {
    product: null
  },
  onLoad(options) {
    const product = products.find((item) => item.id === options.id);
    if (!product) {
      wx.showToast({ title: "商品不存在", icon: "none" });
      return;
    }

    wx.setNavigationBarTitle({ title: product.name });
    this.setData({ product });
  },
  onShareAppMessage() {
    const product = this.data.product;
    return {
      title: product ? `${product.name} - ${store.name}` : store.shareTitle,
      path: product ? `/pages/product-detail/product-detail?id=${product.id}` : "/pages/home/home"
    };
  }
});
```

- [ ] **Step 2: Add product detail config**

Add `miniprogram/pages/product-detail/product-detail.json`:

```json
{
  "navigationBarTitleText": "商品详情",
  "usingComponents": {
    "contact-bar": "/components/contact-bar/contact-bar"
  }
}
```

- [ ] **Step 3: Add product detail markup**

Add `miniprogram/pages/product-detail/product-detail.wxml`:

```xml
<view wx:if="{{product}}" class="page detail-page safe-bottom">
  <view class="detail-cover tone-{{product.imageTone || 'warm'}}">
    <view wx:for="{{product.tags}}" wx:key="*this" class="detail-tag">{{item}}</view>
  </view>

  <view class="detail-main card">
    <view class="detail-name">{{product.name}}</view>
    <view class="detail-price {{product.priceType === 'special' ? 'special' : ''}}">{{product.priceText}}</view>
    <view class="detail-note">花色、尺寸、库存以门店实物为准</view>
  </view>

  <view class="section card detail-section">
    <view class="section-title">产品卖点</view>
    <view wx:for="{{product.highlights}}" wx:key="*this" class="list-item">{{item}}</view>
  </view>

  <view class="section card detail-section">
    <view class="section-title">规格信息</view>
    <view wx:for="{{product.specs}}" wx:key="*this" class="list-item">{{item}}</view>
  </view>

  <view class="section card detail-section">
    <view class="section-title">产品说明</view>
    <view class="description">{{product.description}}</view>
  </view>

  <contact-bar />
</view>
```

- [ ] **Step 4: Add product detail styles**

Add `miniprogram/pages/product-detail/product-detail.wxss`:

```css
.detail-page {
  padding-bottom: 150rpx;
}

.detail-cover {
  display: flex;
  align-items: flex-start;
  min-height: 420rpx;
  padding: 24rpx;
  border-radius: 16rpx;
  box-sizing: border-box;
}

.tone-warm { background: linear-gradient(135deg, #ead7c5, #b85648); }
.tone-deep { background: linear-gradient(135deg, #d6c4a8, #6f3f2c); }
.tone-soft { background: linear-gradient(135deg, #f2dedc, #c87982); }
.tone-rose { background: linear-gradient(135deg, #f1c7c0, #a94c5a); }
.tone-green { background: linear-gradient(135deg, #d8e5cf, #5e8f74); }
.tone-red { background: linear-gradient(135deg, #e3a09a, #9b3430); }
.tone-gold { background: linear-gradient(135deg, #edd59d, #c8872d); }

.detail-tag {
  margin-right: 12rpx;
  padding: 6rpx 16rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.88);
  color: #9b3430;
  font-size: 24rpx;
  font-weight: 700;
}

.detail-main {
  margin-top: 18rpx;
  padding: 26rpx;
}

.detail-name {
  color: #2f2b27;
  font-size: 40rpx;
  font-weight: 800;
}

.detail-price {
  margin-top: 14rpx;
  color: #9b3430;
  font-size: 34rpx;
  font-weight: 800;
}

.detail-price.special {
  color: #c46a20;
}

.detail-note {
  margin-top: 12rpx;
  color: #7a7167;
  font-size: 24rpx;
}

.detail-section {
  padding: 26rpx;
}

.list-item {
  margin-top: 16rpx;
  color: #4a433d;
  font-size: 27rpx;
  line-height: 1.55;
}

.description {
  margin-top: 14rpx;
  color: #4a433d;
  font-size: 27rpx;
  line-height: 1.65;
}
```

- [ ] **Step 5: Commit product detail page**

Run:

```powershell
git add miniprogram/pages/product-detail
git commit -m "feat: 添加商品详情页"
```

## Task 7: Store Page

**Files:**
- Create: `miniprogram/pages/store/store.js`
- Create: `miniprogram/pages/store/store.json`
- Create: `miniprogram/pages/store/store.wxml`
- Create: `miniprogram/pages/store/store.wxss`

- [ ] **Step 1: Add store page JS**

Add `miniprogram/pages/store/store.js`:

```js
const store = require("../../config/store");
const contact = require("../../utils/contact");

Page({
  data: {
    store,
    faqs: [
      "支持到店看实物，花色和尺寸以门店现货为准。",
      "普通商品可微信咨询，特价商品建议尽快到店确认。",
      "第一版小程序不支持线上支付，下单和付款请联系门店。"
    ]
  },
  onShareAppMessage() {
    return {
      title: store.shareTitle,
      path: "/pages/store/store"
    };
  },
  handleWechat() {
    contact.copyWechat();
  },
  handlePhone() {
    contact.callStore();
  },
  handleLocation() {
    contact.openStoreLocation();
  }
});
```

- [ ] **Step 2: Add store page config**

Add `miniprogram/pages/store/store.json`:

```json
{
  "navigationBarTitleText": "门店信息"
}
```

- [ ] **Step 3: Add store page markup**

Add `miniprogram/pages/store/store.wxml`:

```xml
<view class="page store-page">
  <view class="store-hero">
    <view class="store-name">{{store.name}}</view>
    <view class="store-slogan">{{store.slogan}}</view>
  </view>

  <view class="quick-actions">
    <button bindtap="handleWechat">微信咨询</button>
    <button bindtap="handlePhone">电话联系</button>
    <button bindtap="handleLocation">导航到店</button>
  </view>

  <view class="section card info-card">
    <view class="info-row">
      <view class="info-label">门店地址</view>
      <view class="info-value">{{store.address}}</view>
    </view>
    <view class="info-row">
      <view class="info-label">营业时间</view>
      <view class="info-value">{{store.businessHours}}</view>
    </view>
    <view class="info-row">
      <view class="info-label">联系电话</view>
      <view class="info-value">{{store.phone || '正式发布前补充'}}</view>
    </view>
    <view class="info-row">
      <view class="info-label">微信咨询</view>
      <view class="info-value">{{store.wechatId || '正式发布前补充'}}</view>
    </view>
  </view>

  <view class="section card photo-card">
    <view class="section-title">门店实拍</view>
    <view class="store-photo">门头 / 店内实拍图后续替换</view>
  </view>

  <view class="section card faq-card">
    <view class="section-title">常见问题</view>
    <view wx:for="{{faqs}}" wx:key="*this" class="faq-item">{{item}}</view>
  </view>
</view>
```

- [ ] **Step 4: Add store page styles**

Add `miniprogram/pages/store/store.wxss`:

```css
.store-page {
  padding-bottom: 48rpx;
}

.store-hero {
  padding: 34rpx;
  border-radius: 16rpx;
  color: #ffffff;
  background: linear-gradient(135deg, #9b3430, #d58a48);
}

.store-name {
  font-size: 44rpx;
  font-weight: 800;
}

.store-slogan {
  margin-top: 12rpx;
  font-size: 26rpx;
  opacity: 0.94;
}

.quick-actions {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx;
  margin-top: 20rpx;
}

.quick-actions button {
  height: 82rpx;
  line-height: 82rpx;
  padding: 0;
  border-radius: 12rpx;
  background: #ffffff;
  color: #9b3430;
  font-size: 26rpx;
  font-weight: 700;
}

.quick-actions button::after {
  border: 0;
}

.info-card,
.photo-card,
.faq-card {
  padding: 26rpx;
}

.info-row {
  padding: 18rpx 0;
  border-bottom: 1rpx solid #eee7df;
}

.info-row:last-child {
  border-bottom: 0;
}

.info-label {
  color: #7a7167;
  font-size: 24rpx;
}

.info-value {
  margin-top: 8rpx;
  color: #2f2b27;
  font-size: 28rpx;
  line-height: 1.55;
}

.store-photo {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 260rpx;
  margin-top: 18rpx;
  border-radius: 12rpx;
  background: linear-gradient(135deg, #ead7c5, #b85648);
  color: #ffffff;
  font-size: 28rpx;
  font-weight: 700;
}

.faq-item {
  margin-top: 18rpx;
  color: #4a433d;
  font-size: 26rpx;
  line-height: 1.6;
}
```

- [ ] **Step 5: Commit store page**

Run:

```powershell
git add miniprogram/pages/store
git commit -m "feat: 添加门店信息页"
```

## Task 8: Verification and Final Commit

**Files:**
- Modify: `docs/superpowers/specs/2026-05-25-beibei-home-textile-miniprogram-design.md` only if implementation reveals a spec mismatch.

- [ ] **Step 1: Verify JSON parse**

Run:

```powershell
Get-ChildItem -Path miniprogram -Recurse -Filter *.json | ForEach-Object { Get-Content $_.FullName | ConvertFrom-Json | Out-Null }
Get-Content project.config.json | ConvertFrom-Json | Out-Null
```

Expected: exit code `0`, no parse errors.

- [ ] **Step 2: Verify JavaScript modules load**

Run:

```powershell
node -e "const cats=require('./miniprogram/data/categories'); const products=require('./miniprogram/data/products'); const store=require('./miniprogram/config/store'); if(cats.length!==7) throw new Error('bad category count'); if(products.length!==7) throw new Error('bad product count'); if(store.name!=='贝贝家纺') throw new Error('bad store name'); console.log('data ok')"
```

Expected: exit code `0`, output `data ok`.

- [ ] **Step 3: Verify required files exist**

Run:

```powershell
$required = @(
  'project.config.json',
  'miniprogram/app.js',
  'miniprogram/app.json',
  'miniprogram/app.wxss',
  'miniprogram/config/store.js',
  'miniprogram/data/categories.js',
  'miniprogram/data/products.js',
  'miniprogram/utils/contact.js',
  'miniprogram/pages/home/home.js',
  'miniprogram/pages/products/products.js',
  'miniprogram/pages/product-detail/product-detail.js',
  'miniprogram/pages/store/store.js',
  'miniprogram/components/product-card/product-card.js',
  'miniprogram/components/contact-bar/contact-bar.js'
)
$missing = $required | Where-Object { -not (Test-Path $_) }
if ($missing) { $missing; exit 1 }
'files ok'
```

Expected: exit code `0`, output `files ok`.

- [ ] **Step 4: Open in WeChat DevTools**

Manual verification:

1. Open WeChat DevTools.
2. Import `D:\Work\Code\bbjf`.
3. Use test AppID or no AppID mode if the official mini program account is not registered.
4. Confirm pages load: 首页, 产品, 商品详情, 门店.
5. Click 微信咨询, 电话联系, 导航到店 and confirm unconfigured fields show clear modal messages.

Expected: all pages render and contact actions do not crash when real store data is missing.

- [ ] **Step 5: Check git status**

Run:

```powershell
git status --short
```

Expected: no tracked implementation files are left unstaged after the final task commit. The existing machine-level warning about `C:\Users\18620/.config/git/ignore` may still appear and does not block the project files.

- [ ] **Step 6: Push if remote exists**

Run:

```powershell
git remote -v
```

If a remote is configured, run:

```powershell
git push
```

Expected with no remote: `git remote -v` prints nothing and no push is possible. Expected with remote: push exits `0`.

## Self-Review

- Spec coverage: the plan covers original native mini program setup, local data, homepage, category browsing, product detail, store information, contact actions, sharing, empty state, and verification.
- Scope control: payment, cart, membership, SKU, custom appointment, backend, and inventory are not included.
- Placeholder scan: the plan contains no unfinished markers or undefined future implementation steps required for first-version behavior. Real phone, WeChat ID, address, hours, and coordinates remain empty configuration values with visible fallback behavior.
- Type consistency: category IDs, product fields, store fields, contact method names, routes, and component names are consistent across tasks.
