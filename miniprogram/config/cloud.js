module.exports = {
  // If this is empty, WeChat DevTools uses the current default cloud environment.
  // Fill it with your cloud environment ID if DevTools asks for an explicit env.
  envId: "cloud1-d8g1989ulefd55a77",
  usersCollection: "users"
};
